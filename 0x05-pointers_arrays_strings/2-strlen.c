#include "main.h"

/**
 * _strlen - length of a string
 * @s: address of the string
 * return: int
 */
int _strlen(char *s)
{
	int len;
	char str = *s

	len = sizeof(str) / sizeof(str[0]);
	return (len);
}
